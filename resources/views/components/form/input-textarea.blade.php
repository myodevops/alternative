<x-adminlte-textarea name="{{ $name }}" 
                     label="{{ $label }}" 
                     placeholder="{{ $placeholder }}" 
                     data-fieldname="{{ $fieldname }}" 
                     rows="{{ $rows }}"
                     :disabled="$disabled" 
                     :wrap="$wrap"
>
</x-adminlte-textarea>